<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="LinearLayoutTheme">
        <item name="android:layout_marginLeft">16dp</item>
        <item name="android:layout_marginRight">16dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="LinearLayoutTheme.TransParent" parent="LinearLayoutTheme"></style>

    <style name="ForecastCard" parent="LinearLayoutTheme">
        <item name="cardCornerRadius">2dp</item>
        <item name="cardElevation">2dp</item>
        <item name="cardMaxElevation">8dp</item>
        <!--<item name="android:elevation">2dp</item>-->
        <!--<item name="app:cardCornerRadius">2dp</item>-->
        <!--<item name="app:cardMaxElevation">8dp</item>-->
        <!--<item name="app:contentPadding">8dp</item>-->
    </style>

    <style name="AirNowItem">
        <item name="android:paddingBottom">4dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
    </style>
    <style name="AirNowItemImageView">
    </style>

    <style name="AirNowItemTextView">
        <!--<item name="android:layout_marginTop">2dp</item>-->
        <!--<item name="android:layout_marginLeft">2dp</item>-->
    </style>
    <style name="Transparent"></style>

    <style name="QuickWeatherNow">
        <item name="android:paddingTop">16dp</item>
    </style>

</resources>
